import 'package:flutter/material.dart';

class PageBlank extends StatelessWidget {
  const PageBlank({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Color.fromARGB(255, 0, 8, 44),
    );
  }
}
